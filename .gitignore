# .gitignore
# This is a technical file used to blacklist certain files and directories from being tracked in this Git repository.
# It's useful for temporarily excluding files that might cause conflicts or are not needed in the repository, such as incompatible code.
# IMPORTANT: Comments in this file must be placed at the beginning of a line.
# The files listed below are specifically being excluded from this project:

# Removed for crashing:
libsmite-1.0.7d.so
libsmite-1.1.1.so
libsmite-1.2.0.so
libsmite-1.2.1.so

# Incompatible:
libLangit030rc.so
libLangit031rc.so
libLangit032rc.so
libLangit035.so
libLangit040.so
libLangit041.so
libLangit043.so
libaw4c.1.4.1.so
libdukun142.so
libsmusic.so
libsmusic-1.1.2.so
libsmusic-1.1.4.so

# Possible crash on Android 14
libfpschanger.so

# Outdated and superseded by new versions
libLangit044.so

# Blacklist libfun
libfun.so
funny_*.apk

# ThatSkyMod
# Manual method no longer works due to distribution protection
:# See https://discord.com/channels/1247220592919314522/1275512029037199423/1318987652581560370
# Use ThatSkyApp.exe instead
TSM.zip
